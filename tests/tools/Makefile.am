if ENABLE_dhcp
perfdhcp = perfdhcp
endif

if ENABLE_dhcp4
perfdhcp = perfdhcp
endif

if ENABLE_dhcp6
perfdhcp = perfdhcp
endif

if ENABLE_libdns
badpacket = badpacket
endif

SUBDIRS = $(badpacket) $(perfdhcp)
